@model List<TbSlider>
<div class="page-body">

    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">

                <div class="card card-info">
                    <div class="card-body">

                        <a asp-controller="Home" asp-action="EditSlider" class="btn btn-info">New Slider</a>

                        <table class="table table-hover text-nowrap">

                            <thead>
                                <tr>

                                    <th>Slider Id</th>
                                    <th>Slider Image</th>
                                    <th>Actions</th>


                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>

                                        <td>@item.TbSliderId</td>

                                        
                                        <td>
                                            <img src="@string.Format("/Slider/{0}",item.SliderImage)" width="200" height="60" />
                                        </td>
                                        <td>
                                            <a asp-controller="Home" asp-action="EditSlider" asp-route-id="@item.TbSliderId" class="btn btn-warning">Edit</a>
                                            <a asp-controller="Home" asp-action="Delete" asp-route-id="@item.TbSliderId" class="btn btn-primary">Delete</a>
                                        </td>


                                    </tr>

                                }



                            </tbody>
                        </table>
                    </div>
                </div>



            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->




</div>
